---
import { cn } from '../../utils/cn';

interface Props {
	/** Optional short label. Rendered with a single accent rule — used deliberately, not on every section. */
	kicker?: string;
	title: string;
	subtitle?: string;
	as?: 'h1' | 'h2' | 'h3';
	align?: 'left' | 'center';
	tone?: 'light' | 'dark';
	class?: string;
}

const { kicker, title, subtitle, as: Tag = 'h2', align = 'left', tone = 'light', class: className = '' } = Astro.props;

const alignClass = align === 'center' ? 'items-center text-center mx-auto' : 'items-start text-left';
const isDark = tone === 'dark';
const titleColor = isDark ? 'text-surface' : 'text-primary';
const subColor = isDark ? 'text-surface/70' : 'text-muted';
const kickerColor = isDark ? 'text-surface/60' : 'text-primary/55';
---

<div class={cn('flex max-w-2xl flex-col gap-5', alignClass, className)}>
	{
		kicker && (
			<span class={cn('flex items-center gap-3 text-xs font-semibold uppercase tracking-[0.2em]', kickerColor)}>
				<span class="h-px w-8 flex-none bg-accent" aria-hidden="true" />
				{kicker}
			</span>
		)
	}
	<Tag class={cn('font-display text-[2rem] font-semibold leading-[1.08] sm:text-4xl md:text-[2.75rem]', titleColor)}>
		{title}
	</Tag>
	{subtitle && <p class={cn('max-w-[62ch] text-lg leading-[1.6]', subColor)}>{subtitle}</p>}
</div>
